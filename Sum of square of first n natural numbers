Python3 Program to 
# find sum of square of first n natural numbers and print it. 
def squaresum(n) : 
# Iterate i from 1 and n finding square of i and to sum. 

    sum = 0
    for i in range(1, n+1) : 
        sum = sum + (i * i) 
      return sum 
  
# Driven Program 
n = 4
print(squaresum(n)) 
